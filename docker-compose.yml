services:
  app:
    build: .
    container_name: flowboard
    network_mode: host
    ports:
      - "9731:9731"
    volumes:
      - ./data:/data
    environment:
      - TZ=America/Edmonton
    restart: unless-stopped